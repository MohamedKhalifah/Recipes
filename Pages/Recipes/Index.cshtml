@page
@model Recipes.IndexModel
@{
}

<h1> Recipes </h1>

<form method="get">
    <div class="form-group">
        <div class="input-group">
            <input asp-for="SearchTerm" type="search" class="form-control" />
            <span class="input-group-btn">
                <button class="btn btn-default">
                    <i class="glyphicon glyphicon-search"></i>
                </button>
            </span>
        </div>
    </div>
</form>

<table>
    @foreach (var item in Model.Recipes)
    {
        <tr>
            <td> @item.Name </td>
            <td> @item.Category </td>
            <td> @item.Review </td>
            <td>
                <a class="btn btn-lg" asp-page="./Recipe" asp-route-Id="@item.Id">View Recipe</a>
            </td>
            <td>
                <a class="btn btn-block" asp-page="./Edit" asp-route-Id="@item.Id">Edit Recipe</a>
            </td>
        </tr>
    }
</table>

<a asp-page="./Edit" class="btn btn-primary"> Create Recipe </a>